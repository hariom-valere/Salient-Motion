## Sidebar

File: `src/renderer/src/layouts/Sidebar.tsx`

### Behavior
- Collapsible sidebar with navigation links and a device link badge.
- Integrates a `LogoutModal` and calls `useAuth().logout()` on confirm.

### Usage
Included within `AppLayout`.
