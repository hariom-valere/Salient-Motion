## AppLayout

File: `src/renderer/src/layouts/index.tsx`

### Behavior
- Wraps the app with a `Sidebar` and renders an `Outlet` for nested routes.

### Usage
Already integrated in the router as the shell for protected routes.
